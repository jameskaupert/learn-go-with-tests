package iteration

// Takes a character and repeats it repeatCount times, returning the resulting string
func Repeat(character string, repeatCount int) string {
	var repeated string
	for i := 0; i < repeatCount; i++ {
		repeated += character
	}
	return repeated
}
